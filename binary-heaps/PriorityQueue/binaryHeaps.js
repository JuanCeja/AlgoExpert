class Heap {
    constructor(values) {
        this.values = values;
    }

    insert(value) {
        // push the value into the values property on the heap
        // bubble the value up to its correct spot
    }
}